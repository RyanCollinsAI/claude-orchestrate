# Handoff: <one-line title of this work>

Written by <session name> at <timestamp>. The session reading this is the replacement; it has none
of the earlier context and must not ask the human to repeat anything written below.

## Goal (the human's words)

<Quote what they actually asked for, their phrasing, not a summary in your words.>

## Done

<Every finished piece, with the absolute path or commit sha that proves it. One line each.>

- `<absolute path>` - what changed
- commit `abc1234` in `<repo>` - what it shipped

## In progress right now

<What was mid-flight when this file was written. Name the file, the function, and the next edit.>

## Next steps

1. <first thing the replacement should do>
2. ...

## Open questions

<Only real blockers. Empty is a valid answer. Never park a small reversible call here - the
orchestrator answers those.>

## Traps hit

<Anything that cost a wasted turn: a flag that behaves differently than documented, a path that
does not exist, a tool that hangs. Write it so the replacement does not repeat it.>

## Files that matter

<The short list to read first. Absolute paths, with one clause each on why.>
